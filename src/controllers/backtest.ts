// interface Tqueue {
//     pushJob: (name: string, data: any) => Promise<any>;
// }
// importing the schema from zod
// import {zodSchema} from "../schemas/zod.ts";
// import express from "express";

// const app = express();



// using the global error handler
// app.use((err, req, res, next) => {

// });
